<div class="container">
  <div class="row py-5">
    <% if @notifications.exists? %>
      <div class="col-12 col-md-10 col-lg-8 mx-auto py-3 border rounded">
        <%= render @notifications %>
        <%= paginate @notifications %>
      </div>
    <% else %>
      <p>通知はありません</p>
    <% end %>
  </div>
</div>