<%= form_for(customer, url: follow_path(customer.id), method: :post, remote: true) do |f| %>
  <%= hidden_field_tag :followed_id, customer.id %>
  <%= f.submit "お気に入りする", class: "btn btn-success btn-sm" %>
<% end %>
