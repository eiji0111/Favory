<div class="container py-5">
  <nav>
    <div class="nav nav-tabs nav-justified" id="nav-tab" role="tablist">
      <a class="h6 nav-item nav-link active text-dark" id="nav-favo-tab" data-toggle="tab" href="#nav-favo" role="tab" aria-controls="nav-favo" aria-selected="true">
        <span class="d-none d-md-inline-block">ふぁぼした人</span> <i class="bi bi-heart-fill text-danger"></i>
      </a>
      <a class="h6 nav-item nav-link text-dark" id="nav-favarited-tab" data-toggle="tab" href="#nav-favorited" role="tab" aria-controls="nav-love" aria-selected="false">
        <span class="d-none d-md-inline-block">ふぁぼされた人</span> <i class="bi bi-heart text-danger"></i>
      </a>
      <a class="h6 nav-item nav-link text-dark" id="nav-match-tab" data-toggle="tab" href="#nav-match" role="tab" aria-controls="nav-introduction" aria-selected="false">
        <span class="d-none d-md-inline-block">マッチング</span> <i class="bi bi-heart-half text-danger"></i>
      </a>
    </div>
  </nav>
  <div class="tab-content pt-4" id="nav-tabContent">
    <div class="tab-pane fade show active" id="nav-favo" role="tabpanel" aria-labelledby="nav-favo-tab">
      <% if @followeds.blank? %>
        <div class="col py-3">
          ふぁぼしたユーザーはいません。<br>
          気になるユーザーをふぁぼしてみよう！
        </div>
      <% else %>
        <%= render 'public/customers/index', objects: @followeds %>
      <% end %>
    </div>
    <div class="tab-pane fade" id="nav-favorited" role="tabpanel" aria-labelledby="nav-favorited-tab">
      <% if @followers.blank? %>
        <div class="col py-3">
          ふぁぼされていません。<br>
          プロフィールを編集してみよう！
        </div>
      <% else %>
        <%= render 'public/customers/index', objects: @followers %>
      <% end %>
    </div>
    <div class="tab-pane fade" id="nav-match" role="tabpanel" aria-labelledby="nav-match-tab">
      <% if @matchers.blank? %>
        <div class="col py-3">
          マッチングしたユーザーはいません。<br>
          積極的にふぁぼしてみよう！
        </div>
      <% else %>
        <%= render 'public/customers/index', objects: @matchers %>
      <% end %>
    </div>
  </div>
</div>